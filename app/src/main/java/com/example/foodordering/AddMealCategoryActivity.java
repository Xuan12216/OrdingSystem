package com.example.foodordering;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;
import android.widget.ImageView;
import android.view.View;

import com.google.firebase.database.DatabaseReference;
import com.google.firebase.database.FirebaseDatabase;

public class AddMealCategoryActivity extends AppCompatActivity {
    /*
    private String category[];
    private DatabaseReference categoryRef, sellerRef;

    private ImageView backButton;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_add_meal_category);

        //sellerRef = FirebaseDatabase.getInstance().getReference("Users"); // user folder in firebase
        //userID = FirebaseAuth.getInstance().getCurrentUser().getUid().toString();

        //productsRef = FirebaseDatabase.getInstance().getReference().child("Meals"); // product folder in firebase

        categoryRef = FirebaseDatabase.getInstance().getReference().child("Category Collection");


        backButton = (ImageView) findViewById(R.id.button_back_add_category);


        backButton.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                finish();
            }
        });

    }

     */


}